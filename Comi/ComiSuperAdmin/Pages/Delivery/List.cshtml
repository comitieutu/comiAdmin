@page
@model ListModel
@{
    ViewData["Title"] = "List";
}

<h3><i class="fa fa-angle-right"></i> All delivery method</h3>
<div class="row mt">
    <div class="col-md-12">
        <div class="content-panel">
            <table class="table table-striped table-advance table-hover">
                <h4><i class="fa fa-angle-right"></i>All delivery method</h4>
                <hr>
                <thead>
                    <tr>
                        <th><i class="fa fa-bullhorn"></i> Name</th>
                        <th><i class="fa fa-bullhorn"></i> Price</th>
                        <th class="hidden-phone"><i class="fa fa-question-circle"></i> Descrition</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var d in Model.Deliveries)
                    {
                        <tr>
                            <td>@d.Name</td>
                            <td>@d.Price</td>
                            <td>@d.Description</td>
                            <td>
                                <a asp-page="/Payment/Edit" asp-route-id="@d.Id" class="btn btn-primary btn-xs"><i class="fa fa-pencil"></i></a>
                                <a asp-page="/Payment/List" asp-route-id="@d.Id" asp-page-handler="Delete" class="btn btn-danger btn-xs"><i class="fa fa-trash-o "></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

